<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/timejob"
        android:label="@string/app_name"
        android:roundIcon="@drawable/timejob"
        android:supportsRtl="true"
        android:theme="@style/Theme.TimeJob"
        tools:targetApi="31">
        <activity
            android:name=".view.ActivityRelatorio"
            android:exported="false"
            android:label="@string/title_activity_relatorio"
            android:theme="@style/Theme.TimeJob" />
        <activity
            android:name=".view.ActivityAdicionar"
            android:exported="false"
            android:label="@string/title_activity_adicionar"
            android:theme="@style/Theme.TimeJob" />
        <activity
            android:name=".view.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.TimeJob">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>